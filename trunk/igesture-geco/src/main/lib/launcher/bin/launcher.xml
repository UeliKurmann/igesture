<project name="geco" default="geco" basedir=".">

    <property name="base.dir" value="${basedir}/.."/>
    <property name="lib.dir" value="${base.dir}/lib"/>
    <property name="log.dir" value="${base.dir}/log"/>

    <path id="base.class.path">
        <pathelement path="${etc.dir}"/>
        <pathelement path="${base.dir}"/>
        <fileset dir="${lib.dir}" includes="*.jar"/>
    </path>

    <target name="geco">
        <mkdir dir="${log.dir}" />
        <launch classname="org.ximtec.igesture.geco.Geco">
            <classpath refid="base.class.path"/>
            <syspropertyset>
                <sysproperty key="log.dir" file="${log.dir}"/>
                <sysproperty key="java.library.path" file="${lib.dir}"/>
            </syspropertyset>
        </launch>
    </target>
</project>